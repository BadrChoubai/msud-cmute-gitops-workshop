apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: paper
spec:
  secretName: grafana-tls
  dnsNames:
    - grafana.badrchoubai.mc.labs.cmute.cloud
  issuerRef:
    name: letsencrypt-do
    kind: ClusterIssuer
